At first when i downloaded the challenge file it doesnt contained any extension so i used file command to know which type of file it was and i came to know that it was a tar archive so i used tar -xf command to extract it. The x indicate extract and f indicate the filename is gonna be given as next word.After extracting it i got a file as task.bz2 and i came to know that it was a bzip file so i extracted it usinng the command bunzip task.bz2. Then i checked the file it was a gzip file so i extracted it after renaming it with the file extension since the extracted bzip file doesnt contained any extension.After repeating this extraction 1,2 times i got access to other two files inside it which was challenge.tar and heheh challenge.tar was tar archive while other was a compressed data. To chack what compressed data it is i had gone to extract heheh file.heheh file was a gzip file which doesnt had an extenseion i renamed it and extracted it using the same command gunzip. After that i got .heheh.tar i extracted it too and got a hiddden_file when i checked it tit also doesnt contained extension so i used file command to know the type of file and when i came to know that it is a gzip file i extracted it the same way as earlier.After that also it doesnt contained extension but it was a bzip2 file. i extracted it using the command bunzip2. Alll these extractions were done only after renaming the file with appropriate extensions otherswise the command wont work.After extracting that when i checked i again came to gzip file but now i noticed the size had change so i extracted it again it changed to bzip2 extracted again same way and it got back to gzip by repeating this process a couple of time atlast i got the file as ASCII text.After that i used the cat command to see what the text is and there i got the required flag.
THE FLAG IS:ACM{too__eazyy_right}
