At first i looked into the file but it was not executable i used the linux to execute the file. 
I used the command chmod +x to execute the file after execution i just used cat to see what is inside the file
I opened the file using ./password_protected command but i saw that i require a password there so i used cat filename to see whats inside the file
I saw some words and i tried those words as password and at last got YouAreTooLazy as paassword and hence got the flag
